[package]
name = "detector"
version = "0.1.0"
edition = "2021"
description = "Auto-detection of games via Steam, emulators, and native launchers"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
tracing = { workspace = true }
steamlocate = "2.1.0"

[target.'cfg(windows)'.dependencies]
winreg = "0.52"
